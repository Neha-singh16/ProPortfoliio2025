# Copy this file to .env.local and fill in real credentials

## Option A: SMTP (Mailtrap, Gmail app password, or other provider)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

## Option B: Resend (no SMTP needed)
RESEND_API_KEY=
# Optional: RESEND_TO can override recipient; otherwise uses TO_EMAIL
RESEND_TO=

# Common overrides
FROM_EMAIL=
TO_EMAIL=
