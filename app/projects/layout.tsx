import type { ReactNode } from "react"

export const metadata = {
  title: "Case Studies",
  description: "Detailed case studies of my projects and their impact.",
}

export default function ProjectsLayout({ children }: { children: ReactNode }) {
  return children
}
